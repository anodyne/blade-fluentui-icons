{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "packageManager": "bun@1.4.2",
    "scripts": {
        "generate-enum": "bun bin/generate-enum.mjs",
        "optimize-icons": "svgo --folder resources/svg --recursive --multipass --quiet"
    },
    "devDependencies": {
        "svgo": "^4.1.0"
    }
}
